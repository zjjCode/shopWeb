# 电商商城系统 · 设计交付总览（v2）

> 状态：**设计阶段 100% 完成，四份设计文档全部对齐到 v2.0**。未写任何业务代码，待客户确认后进入实现阶段。
> 技术栈：Vue 3 + Vite / Node.js + Express / MySQL 8.0 + Prisma / Redis + BullMQ

---

## 一、四项变更的落实情况（客户 2026-09-02 提出）

| # | 客户要求 | PRD | 架构 `02` | 数据库 `03` | 流程 `04` |
| --- | --- | --- | --- | --- | --- |
| ① | 做优惠券 / 促销 | ✅ | ✅ | ✅ | ✅ **F13 + F5/F6 已加** |
| ② | 做动态角色管理台 | ✅ | ✅ | ✅ | ✅ 后台流程统一 `authorize('资源:操作')` |
| ③ | 支付接支付宝 + 微信 + **银行卡** | ✅ | ✅ | ✅ | ✅ **F6 渠道路由 + 聚合服务商** |
| ④ | 用户维度余额账户 + 充值 + 消费 | ✅ | ✅ | ✅ | ✅ **F14/F15 + F6.6** |
| ⑤ | （追加更正）**取消混合支付**，单一支付方式 | ✅ | ✅ | ✅ | ✅ **F6/F9.3 单一路由** |

**结论**：需求、架构、数据库、流程**四层已全部到位并互相对齐（均 v2.0）**。`04-flows.md` 已补齐 v2 内容（1839 行，45 处 v2 标记），原 v1 缺口全部闭合。

---

## 二、文档清单

| 文档 | 行数 | 状态 |
| --- | --- | --- |
| `docs/01-PRD.md` | 909 | ✅ v1 基线（未受影响部分继续有效） |
| `docs/05-PRD-变更.md` | 1303 | ✅ **v1.1** 增量 PRD，四项变更 + 取消混合支付 |
| `docs/02-architecture.md` | 1975 | ✅ **v2.0**，88 处 v2 标记 |
| `docs/03-database.md` | 2833 | ✅ **v2.0**，38 个 Prisma 模型（v1 为 24 个） |
| `docs/04-flows.md` | 1839 | ✅ **v2.0**，45 处 v2 标记（F13/F14/F15 新增 + F6 异步重做 + F5/F9/F11/F12 更新） |

---

## 三、v2 关键设计决策

| 项 | 决策 |
| --- | --- |
| **支付方式** | **单选**，一笔订单只用一种：余额 / 支付宝 / 微信 / 银行卡。**不做混合支付** |
| **退款去向** | 余额支付 → 退回余额账户；渠道支付 → 原路退回。按 `pay_method` 单一路由 |
| **金额恒等式** | `应付总额 = 商品金额 - 优惠金额 + 运费`（E1~E10 共 10 条恒等式，代码中逐条断言） |
| **优惠分摊** | 优惠**必须分摊到订单行**，尾差计入金额最大项。这是部分退款能算清的前提 |
| **部分退款** | **提为 P0**（产品经理上调）。取消混合支付后无跨来源拆分，成本大幅下降，而"一单多件退一件"是高频售后 |
| **记账模型** | **方案 A+**：单式流水 + 对手方 + 交易组号。`tx_group_no` 绑定「负债结转对」 |
| **余额本质** | 平台对用户的**负债**。充值时钱进平台账户、同时记负债；消费时把负债转为收入确认（同 `tx_group_no` 下 3 条流水） |
| **动态 RBAC** | 四表 + 约 50 个权限点（`资源:操作`）。敏感权限绕过 60s 缓存实时判定。**代码内禁止按角色名判断** |
| **支付回调** | **统一异步**：验签 → 落库 → 投递 BullMQ → 立即应答（微信强制 5s）。mock 与支付宝也走异步，避免两条代码路径 |
| **银行卡选型** | **推荐聚合支付服务商**（拉卡拉/收钱吧），非直连银联。理由：银联进件 4~8 周超工期；托管收银台规避 PCI-DSS |

---

## 四、已闭合的 `docs/04-flows.md` 修订（v2 全部完成）

| 流程 | 动作 | 要点 |
| --- | --- | --- |
| **F6 支付** | ✅ 全量重做 | 同步 → **异步回调链路（BullMQ）**；幂等三重保险；余额支付分支不走适配器（F6.5/6.6/6.7） |
| **F13 优惠券** | ✅ 新增 | 领取（`issued_count` 原子扣减）、使用、**冻结/解冻与订单同事务** |
| **F14 余额充值** | ✅ 新增 | 复用 `payments`（`biz_type=RECHARGE`）；充值成功的双账户记账（负债+） |
| **F15 余额支付** | ✅ 新增 | 事务内扣余额 + 负债结转对；退款退回余额 |
| **F5 下单** | ✅ 更新 | 优惠计算引擎调用点、券冻结、加锁顺序**先券后库存** |
| **F9 退款** | ✅ 更新 | 部分退款（提 P0）+ 按行退已分摊优惠 + 按 `pay_method` 单一路由 |
| **F11 对账** | ✅ 更新 | 新增「用户余额账户」与「平台账户」两种视图（L1/L2/L3 勾稽） |
| **F12 状态机** | ✅ 更新 | 加券冻结/解冻、余额退款的跃迁说明 |
| 附录 A | ✅ 更新 | 新增「余额负债确认收入的记账正确性」风险项（④） |

---

## 五、实现阶段高风险点

| 风险 | 缓解措施 |
| --- | --- |
| **并发库存超卖** | Redis 锁仅削峰，权威判定是事务内 CAS（`WHERE available >= ? AND version = ?`）；多 SKU 按 `sku_id` 升序防死锁；`CHECK` 兜底 |
| **回调幂等与余额快照** | 幂等表 + 支付单状态机 + 流水唯一索引三重保险；`FOR UPDATE` 锁账户串行记账保证 before/after 连续 |
| **余额负债确认收入** | 同 `tx_group_no` 下 3 条流水（用户余额 OUT + 平台 IN 确认收入 + 平台 OUT 冲减负债），缺一条则负债与收入漂移 |
| **优惠分摊尾差** | `MoneyUtil.allocate` 按权重分摊 + 尾差计入最大项，断言 E3 严格成立 |
| **长事务** | 事务内禁止任何第三方 HTTP；渠道 `closePayment`/`refund` 一律提交后异步；事务超时 10s |

---

## 六、下一步

1. **`docs/04-flows.md` 已补完**（v2 设计四层全部对齐），等待客户确认整体设计
2. 客户确认后进实现阶段，建议拆两条并行线：
   - 交易主线：脚手架 → 迁移/种子 → 用户域 → 商品域 → 购物车 → 订单 → 支付 → 退款
   - 新增子域：优惠券/促销 → 动态 RBAC → 余额/充值 → 支付渠道接入
3. 集成测试重点：并发抢库存、重复回调幂等、优惠分摊尾差、余额负债对账

**积分体系**为第二阶段，已预留扩展点（订单完成事件钩子、`account_type` 多账户、`PriceService` 任意扣减项），本期不实现。
